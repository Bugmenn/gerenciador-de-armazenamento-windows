#pragma once

// IDs de recurso do executavel. Mantido separado do resto de include/ porque
// e' consumido tanto pelo .rc (resource compiler) quanto pelo C++ (App.cpp,
// TrayIcon.cpp) para referenciar o mesmo icone.
#define IDI_APP_ICON 101
